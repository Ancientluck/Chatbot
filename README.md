It consists of a clone of the GPT-2 model
-> It consists of the simple Bigram Model which have the forward and backward pass
-> It contains the GPT column which leverages the multiHeadded Attention model
